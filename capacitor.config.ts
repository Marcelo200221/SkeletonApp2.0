import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'SkeletonApp2.0',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
